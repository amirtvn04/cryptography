# 🔐 Anti-censorship

یک ابزار رمزنگاری متن که داده‌های شما را به دنباله‌ای از **کلمات فارسی و ایموجی** تبدیل می‌کند. این روش باعث می‌شود خروجی رمزشده شبیه یک متن معمولی به نظر برسد و کمتر مشکوک باشد.

## ✨ ویژگی‌ها

- 🔒 **رمزنگاری قوی**: استفاده از AES-256-GCM
- 🇮🇷 **خروجی فارسی**: تبدیل به 128 کلمه فارسی + 128 ایموجی
- 🗜️ **فشرده‌سازی هوشمند**: Gzip برای کاهش حجم
- 🔑 **مبتنی بر پسورد**: PBKDF2 با 150,000 تکرار
- 🌐 **کاملا Client-Side**: هیچ داده‌ای به سرور ارسال نمی‌شود
- 📱 **سازگار با مرورگرهای مدرن**: بدون نیاز به کتابخانه خارجی

## 🚀 نحوه استفاده

### رمزنگاری متن

1. متن خود را در کادر **ورودی** وارد کنید
2. (اختیاری) یک **کلید** قوی وارد کنید
   - اگر کلید وارد نکنید، فقط encoding انجام می‌شود (بدون رمزنگاری!)
3. دکمه **رمزنگاری** را بزنید
4. خروجی در کادر پایین نمایش داده می‌شود

**مثال خروجی:**
```
آب آب آب زمان آسمان ابر 💙 شب طبیعت 🌟 گل نور دریا...
```

### رمزگشایی متن

1. توکن‌های رمز شده را در کادر **ورودی** قرار دهید
2. همان **کلید** را که در رمزنگاری استفاده کرده‌اید وارد کنید
3. دکمه **رمزگشایی** را بزنید
4. متن اصلی بازیابی می‌شود

## 🔧 نحوه کار

### معماری کلی

```
متن اصلی
    ↓
UTF-8 Encoding
    ↓
Gzip Compression (اختیاری)
    ↓
AES-256-GCM Encryption (با پسورد)
    ↓
Packaging (Version + Flags + Salt + IV + Data)
    ↓
Token Encoding (هر بایت = یک توکن فارسی/ایموجی)
    ↓
خروجی نهایی: "آب گل نور شادی..."
```

### الگوریتم‌های مورد استفاده

#### 1. **PBKDF2** (Password-Based Key Derivation)
- تبدیل پسورد به کلید 256 بیتی
- 150,000 تکرار برای مقاومت در برابر حملات brute-force
- Salt تصادفی 16 بایتی برای هر رمزنگاری

#### 2. **AES-256-GCM** (Advanced Encryption Standard)
- رمزنگاری با کلید 256 بیتی
- حالت GCM برای احراز اصالت داده
- IV تصادفی 12 بایتی
- Authentication Tag 16 بایتی برای تشخیص دستکاری

#### 3. **Gzip Compression**
- فشرده‌سازی قبل از رمزنگاری
- فقط در صورتی استفاده می‌شود که حجم کاهش یابد

### ساختار داده

**بدون پسورد:**
```
[Version: 1 byte][Flags: 1 byte][Compressed Data]
```

**با پسورد:**
```
[Version: 1 byte][Flags: 1 byte][Salt: 16 bytes][IV: 12 bytes][Encrypted Data + Tag: variable]
```

**Flags:**
- بیت 0: رمز شده (1) یا خیر (0)
- بیت 1: فشرده شده (1) یا خیر (0)

## 💡 توصیه‌های امنیتی

### پسورد قوی چیست؟

**❌ ضعیف:**
```
123
password
12345678
salam
```

**✅ قوی:**
```
کوه‌های_البرز_در_زمستان_برفی_هستند_1402
Tr0pic@l-St0rm#9&Pers!an*Sky
مرغ_سحر_با_همه_هوشیاری_نیست_آگاه_🦅
```

### بهترین روش‌ها

1. **حداقل 20 کاراکتر** استفاده کنید
2. از **ترکیب کلمات با معنا** استفاده کنید (Passphrase)
3. **اعداد، نمادها، و زبان‌های مختلف** را ترکیب کنید
4. پسورد را در **جای امن** ذخیره کنید
5. برای اطلاعات مهم، از **مدیر رمز عبور** استفاده کنید

## 🔍 جزئیات فنی

### فضای توکن (256 عدد)

| محدوده | نوع | تعداد |
|--------|-----|-------|
| 0-127 | کلمات فارسی | 128 |
| 128-255 | ایموجی | 128 |

**مثال نگاشت:**
- بایت `0x00` (0) → `"آب"`
- بایت `0x01` (1) → `"آسمان"`
- بایت `0x80` (128) → `"😀"`
- بایت `0xFF` (255) → `"🌤"`

### کدهای خطا

| پیام خطا | دلیل |
|---------|------|
| `ورودی خالی است` | متن یا توکن وارد نشده |
| `توکن نامعتبر` | کلمه یا ایموجی شناخته نشده |
| `داده کافی نیست` | توکن‌های کمتر از 4 بایت |
| `داده ناقص/دستکاری شده` | طول داده مطابقت ندارد |
| `کلید نادرست است` | پسورد اشتباه یا داده آسیب دیده |
| `نسخه پشتیبانی نمی‌شود` | فرمت داده قدیمی یا جدیدتر |

## 🧪 مثال‌های استفاده

### مثال 1: رمزنگاری ساده

```javascript
Input:    "این یک پیام محرمانه است"
Password: (خالی)

Output:   "آب آب آب امید باد روز گل..."
Note:     ⚠️ فقط encoded شده، رمز نشده!
```

### مثال 2: رمزنگاری کامل

```javascript
Input:    "اطلاعات مهم من"
Password: "Super$ecret2024"

Output:   "آب آب آب برف آسمان ابر 💙 شب طبیعت 🌟 گل..."
Note:     ✅ رمز شده با AES-256-GCM
```

### مثال 3: رمزگشایی

```javascript
Input:    "آب آب آب برف آسمان ابر 💙 شب طبیعت 🌟 گل..."
Password: "Super$ecret2024"

Output:   "اطلاعات مهم من"
```

## 🐛 عیب‌یابی

### خطای "کلید نادرست است"

- پسورد اشتباه است
- داده دستکاری شده است
- فایل آسیب دیده است

### خطای "توکن نامعتبر"

- کپی/پیست اشتباه انجام شده
- کاراکترهای اضافی وارد شده
- از فاصله‌های اضافی اطمینان حاصل کنید

### عملکرد کند

- طبیعی است! PBKDF2 با 150,000 تکرار زمان‌بر است
- معمولا 0.1-0.5 ثانیه طول می‌کشد
- برای فایل‌های بزرگ (>100KB) کندتر است
